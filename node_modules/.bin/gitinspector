#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/python" ]; then
  "$basedir/python"  "$basedir/../gitinspector/gitinspector.py" "$@"
  ret=$?
else 
  python  "$basedir/../gitinspector/gitinspector.py" "$@"
  ret=$?
fi
exit $ret
